# Tuples 
# Similar to lists 
# Cannot modify tuples 

numbers = (1, 2, 3)

# Only two methods that can be used - count and index 

# Can index items in list, but cannot change them 
print(numbers[0])
